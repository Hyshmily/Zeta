name: Publish to GitHub Packages

on:
  release:
    types: [created, published]
  workflow_dispatch:

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: maven

      - name: Build all JARs
        run: mvn clean package -DskipTests --no-transfer-progress

      - name: Package release zip & tar.gz
        run: |
          VER=${{ github.ref_name }}
          PKDIR="packaging/hotkey-worker-${VER}"
          mkdir -p "${PKDIR}/lib"
          mkdir -p "${PKDIR}/docker"
          cp worker/target/hotkey-worker-*.jar "${PKDIR}/lib/"
          cp worker/Dockerfile worker/docker-compose.yml "${PKDIR}/docker/"
          cp -r docs "${PKDIR}/"
          cp README.md README.zh.md LICENSE "${PKDIR}/"
          (cd packaging && zip -r "../hotkey-worker-${VER}.zip" "hotkey-worker-${VER}")
          (cd packaging && tar czf "../hotkey-worker-${VER}.tar.gz" "hotkey-worker-${VER}")

      - name: Publish common to GitHub Packages
        run: mvn deploy -P release -DskipTests --no-transfer-progress
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload JARs and packages to Release
        uses: softprops/action-gh-release@v2
        with:
          files: |
            common/target/hotkey-*.jar
            common/target/hotkey-*-sources.jar
            common/target/hotkey-*-javadoc.jar
            worker/target/hotkey-worker-*.jar
            hotkey-worker-*.zip
            hotkey-worker-*.tar.gz
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
